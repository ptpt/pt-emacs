pt-emacs is my personal Emacs configuration. It redefines some
commands in simple.el and provides some useful commands.

To install it:

1.  Download pt-simple.el
2.  Add (require 'pt-simple) to your ~/.emacs
